apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  labels:
    app: tracing
    app.kubernetes.io/name: tracing
  name: tracing
  namespace: istio-system
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  rules:
    - host: tracing-istio.demo.mzdev.be
      http:
        paths:
          - backend:
              serviceName: tracing
              servicePort: 80
            path: /
  tls:
    - hosts:
        - tracing-istio.demo.mzdev.be
      secretName: tracing-mzdev-tls
