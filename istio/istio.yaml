# istio

grafana:
  enabled: true

ingress:
  enabled: true

tracing:
  enabled: true
  ingress:
    enabled: true
    # hosts:
    #   - tracing.istio.nalbam.com
  jaeger:
    ingress:
      enabled: true
      # hosts:
      #   - jaeger.istio.nalbam.com

kiali:
  enabled: true
  ingress:
    enabled: true
    # hosts:
    #   - kiali.istio.nalbam.com

servicegraph:
  enabled: true
  ingress:
    enabled: true
    # hosts:
    #   - servicegraph.istio.nalbam.com
